<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lineapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LineApp">
        <activity android:name=".appgame.Activiyapp_game"></activity>
        <activity android:name=".lineapp5.Activiyapp5" />
        <activity android:name=".lineapp4.Activiyapp4" />
        <activity android:name=".lineapp3.Activiyapp3" />
        <activity android:name=".lineapp2.Activiyapp2" />
        <activity android:name=".lineapp1.Activiyapp1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>